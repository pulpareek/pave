# PAVE FastAPI backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Databricks SDK — the provisioning engine
databricks-sdk>=0.40.0

# Lakebase (async Postgres) — operational state
asyncpg>=0.29.0

# Validation
pydantic>=2.5.0

# HTTP client (system tables / misc)
httpx>=0.26.0
